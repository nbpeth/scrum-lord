export const citizensContainerSx = {
  width: "100%",
  minWidth: 0,
  maxWidth: "100%",
  alignItems: "stretch",
  justifyContent: "flex-start",
};

export const voteCardContainerSx = {
  mt: 1.25,
  mx: 0,
  width: "100%",
  minWidth: 0,
  maxWidth: "100%",
};

export const emptyRoomSx = (theme) => ({
  fontStyle: "italic",
  color: theme.palette.grey[700],
});
