export const lurkerBoxSx = { paddingLeft: "15px" };

export const deleteLurkerIconSx = { cursor: "pointer" };

export const scrumLordIconSx = { color: "warning.main", mr: 0.25 };

export const lurkerNameSx = (theme) => ({
  whiteSpace: "nowrap",
  color: theme.palette.grey[500],
});
